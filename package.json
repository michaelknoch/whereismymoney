{
  "name": "whereismymoney",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "babel app.js --presets=es2015 --watch --out-dir build",
    "build": "babel app.js --presets=es2015 --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Knoch",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^1.11.0",
    "csvtojson": "^1.1.5"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  }
}
