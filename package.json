{
  "name": "whereismymoney",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "babel *.js --watch --out-dir build",
    "build": "babel *.js --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Knoch",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  }
}
